---
import { getLangFromUrl, getAlternateUrls } from '../i18n/utils';

interface Props {
  title: string;
  description: string;
  image?: string;
  type?: 'website' | 'article';
  canonicalURL?: string;
  noIndex?: boolean;
}

const {
  title,
  description,
  image,
  type = 'website',
  canonicalURL = new URL(Astro.url.pathname, Astro.site).href,
  noIndex = false,
} = Astro.props;

const lang = getLangFromUrl(Astro.url);
const siteName = 'Juan Adrián García García — adriangg.dev';
const fullTitle = `${title} | ${siteName}`;
const absoluteImage = image ? new URL(image, Astro.site).href : undefined;
const ogLocale = lang === 'es' ? 'es_ES' : 'en_US';
const altOgLocale = lang === 'es' ? 'en_US' : 'es_ES';

const alternates = Astro.site ? getAlternateUrls(Astro.url, Astro.site) : [];
---

<!-- Primary meta -->
<title>{fullTitle}</title>
<meta name="description" content={description} />
<meta name="author" content="Juan Adrián García García" />
<meta name="theme-color" content="#0a0a0a" />
<meta name="robots" content={noIndex ? 'noindex, nofollow' : 'index, follow'} />
<link rel="canonical" href={canonicalURL} />

<!-- Hreflang alternates -->
{alternates.map(({ lang: hLang, href }) => (
  <link rel="alternate" hreflang={hLang} href={href} />
))}
<link rel="alternate" hreflang="x-default" href={alternates.find(a => a.lang === 'es')?.href ?? canonicalURL} />

<!-- Open Graph -->
<meta property="og:type" content={type} />
<meta property="og:url" content={canonicalURL} />
<meta property="og:title" content={fullTitle} />
<meta property="og:description" content={description} />
{absoluteImage && <meta property="og:image" content={absoluteImage} />}
{absoluteImage && <meta property="og:image:width" content="1200" />}
{absoluteImage && <meta property="og:image:height" content="630" />}
{absoluteImage && <meta property="og:image:alt" content={description} />}
<meta property="og:site_name" content={siteName} />
<meta property="og:locale" content={ogLocale} />
<meta property="og:locale:alternate" content={altOgLocale} />

<!-- Twitter / X Cards -->
<meta name="twitter:card" content={absoluteImage ? 'summary_large_image' : 'summary'} />
<meta name="twitter:site" content="@adriangar99_" />
<meta name="twitter:creator" content="@adriangar99_" />
<meta name="twitter:url" content={canonicalURL} />
<meta name="twitter:title" content={fullTitle} />
<meta name="twitter:description" content={description} />
{absoluteImage && <meta name="twitter:image" content={absoluteImage} />}
{absoluteImage && <meta name="twitter:image:alt" content={description} />}

<!-- Sitemap discovery -->
<link rel="sitemap" href="/sitemap-index.xml" />
